# atuin — lịch sử lệnh thông minh (Ctrl-R)
auto_sync = false          # chỉ lưu local, không đồng bộ server
update_check = false
style = "compact"
inline_height = 20
show_preview = true
enter_accept = false       # Enter chỉ đưa lệnh ra prompt (không tự chạy) cho an toàn
filter_mode_shell_up_key_binding = "session"
